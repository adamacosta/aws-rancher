defaultNamespace: bookinfo
name: bookinfo
dependsOn:
  - name: cert-manager
  - name: external-dns
  - name: issuer
helm:
  chart: ./chart
targetCustomizations:
  - name: prod
    helm:
      values:
        gateway:
          enabled: true
    clusterSelector:
      env: prod