defaultNamespace: bookinfo
dependsOn:
  - name: cert-manager
  - name: external-dns
  - name: issuer
helm:
  chart: ./chart
targetCustomizations:
  helm:
    values:
      gateway:
        enabled: true
  clusterSelector:
    env: prod