apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata:
  name: rgs-demo
  namespace: fleet-default
spec:
  repo: "git@github.com:adamacosta/fleet-demo"
  branch: main
  clientSecretName: ssh-key
  targets:
    - name: all
      clusterSelector: {}